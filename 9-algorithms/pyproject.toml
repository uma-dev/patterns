[project]
name = "algorithms-classification"
version = "0.1.0"
description = "Classification comparison with sklearn + OpenML"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.26",
  "pandas>=2.0",
  "scikit-learn>=1.4",
]

[tool.uv]
package =true 
